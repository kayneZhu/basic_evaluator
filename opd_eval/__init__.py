"""Project evaluation layer for the OPD teachable-frontier paper.

Framework engine and previous-paper adaptors stay in ``core/`` / ``adaptors/``.
This package owns the INTERFACE.md contract: C.1 is in
``adaptors/c1_prompt_mixin.py``; sample jsonl, stats, checkpoints, and the
roster live here.
"""
